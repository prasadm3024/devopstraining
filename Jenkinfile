pipeline {
    agent  any
    
    tools {
        maven 'Maven'
        jdk 'JDK'
    }
    stages {
    checkout scm
        stage ('Initialize') {
            steps {
                withMAVEN(
            }
        }

        stage ('Build') {
            steps {
                bat 'cd payslip & mvn clean install' 
            }
            post {
                success {
                    junit 'payslip/target/surefire-reports/**/*.xml' 
                }
            }
        }
    }
}
